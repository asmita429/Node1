// import { sum, g } from "./math.js";
import { generate } from "random-words";

// console.log(sum(9, 4));
// console.log("value of G = ", g);

// generated random words.
console.log(generate());

// do not push node_modules in git, it can be simply
// installed again by navigating dependencies on package.json file
